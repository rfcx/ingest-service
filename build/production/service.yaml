kind: Service
apiVersion: v1
metadata:
  name: ingest-service-service
  namespace: production
spec:
  selector:
    app: ingest-service
  type: ClusterIP
  ports:
  - name: node
    port: 80
    targetPort: 80
